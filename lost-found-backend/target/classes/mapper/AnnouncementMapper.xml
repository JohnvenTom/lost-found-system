<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.lostfound.dao.AnnouncementMapper">

    <resultMap id="AnnouncementResultMap" type="com.campus.lostfound.entity.Announcement">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="user_id" property="userId"/>
        <result column="is_top" property="isTop"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="username" property="username"/>
    </resultMap>

    <select id="selectById" resultMap="AnnouncementResultMap">
        SELECT a.*, u.username
        FROM t_announcement a
        LEFT JOIN t_user u ON a.user_id = u.id
        WHERE a.id = #{id}
    </select>

    <select id="selectList" resultMap="AnnouncementResultMap">
        SELECT a.*, u.username
        FROM t_announcement a
        LEFT JOIN t_user u ON a.user_id = u.id
        <where>
            <if test="status != null">
                AND a.status = #{status}
            </if>
        </where>
        ORDER BY a.is_top DESC, a.create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_announcement (title, content, user_id, is_top, status)
        VALUES (#{title}, #{content}, #{userId}, #{isTop}, #{status})
    </insert>

    <update id="updateById">
        UPDATE t_announcement
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="isTop != null">is_top = #{isTop},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_announcement WHERE id = #{id}
    </delete>
</mapper>
